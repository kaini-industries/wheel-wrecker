#pragma once

// Arduino entry points are kept behind uniquely named functions so the IDE's
// sketch preprocessor only needs to process the small .ino adapter.
void wheelWreckerSetup();
void wheelWreckerLoop();
